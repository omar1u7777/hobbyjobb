import { Link } from 'react-router-dom';
import { formatPrice, formatDate } from '../../utils/formatters.js';
export default function JobCard({ job }) {
  return <Link to={'/jobs/' + job.id} style={{ background: 'var(--white)', borderRadius: 'var(--r)', border: '1px solid var(--border)', padding: '20px 22px', display: 'flex', alignItems: 'center', gap: 18, textDecoration: 'none', color: 'inherit', transition: 'all .15s' }}><div style={{ width: 48, height: 48, borderRadius: 10, background: 'var(--blue-light)', display: 'flex', alignItems: 'center', justifyContent: 'center', fontSize: 22 }}>{job.category?.icon ?? '📌'}</div><div style={{ flex: 1, minWidth: 0 }}><div style={{ fontSize: 15, fontWeight: 700, marginBottom: 4 }}>{job.title}</div><div style={{ display: 'flex', gap: 12, flexWrap: 'wrap' }}>{job.location && <span style={{ fontSize: 13, color: 'var(--muted)' }}>📍 {job.location}</span>}{job.category && <span style={{ fontSize: 11, fontWeight: 700, background: 'var(--blue-light)', color: 'var(--blue)', borderRadius: 4, padding: '2px 8px' }}>{job.category.name}</span>}</div></div><div style={{ textAlign: 'right' }}><div style={{ fontSize: 20, fontWeight: 800, color: 'var(--blue)' }}>{formatPrice(job.price)}</div></div></Link>;
}
