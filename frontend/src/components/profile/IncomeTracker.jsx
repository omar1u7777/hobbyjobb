import { formatPrice } from '../../utils/formatters.js';
export default function IncomeTracker({ totalYear = 0, pct = 0, isNear = false }) {
  const isAt = totalYear >= 30000; const remaining = Math.max(30000 - totalYear, 0);
  return <div style={{ background: 'var(--white)', borderRadius: 14, padding: 24, boxShadow: 'var(--sh-lg)', border: '1px solid var(--border)' }}><h3 style={{ fontSize: 15, fontWeight: 700, marginBottom: 16 }}>Hobbyinkomst {new Date().getFullYear()}</h3><div style={{ fontSize: 34, fontWeight: 900, marginBottom: 2 }}>{formatPrice(totalYear)}</div><p style={{ fontSize: 13, color: 'var(--muted)', marginBottom: 18 }}>av max 30 000 kr/ar</p><div style={{ background: 'var(--border)', height: 12, borderRadius: 6, overflow: 'hidden', marginBottom: 8 }}><div style={{ width: pct + '%', height: '100%', background: isAt ? 'var(--red)' : isNear ? 'var(--yellow)' : 'var(--blue)', borderRadius: 6, transition: 'width .5s' }} /></div><span style={{ background: isAt ? 'var(--red-light)' : isNear ? 'var(--yellow-light)' : 'var(--green-light)', color: isAt ? 'var(--red)' : isNear ? 'var(--yellow-text)' : 'var(--green-text)', borderRadius: 6, padding: '5px 12px', fontSize: 12, fontWeight: 700 }}>{isAt ? 'Grans nadd' : isNear ? 'Nara gransen' : 'OK'}</span></div>;
}
